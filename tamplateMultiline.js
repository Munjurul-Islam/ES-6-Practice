const firstName = 'Munjurul';
const lastName = 'Islam';
const fullName = firstName + ' ' + lastName;        /**use whitespace between 2 varible */
console.log(fullName)
const fullName2 = `${firstName} ${lastName} is a good boy`  /**es6 template method */
console.log(fullName2)
const calculate = `${40+ 30-7}, tk cost for every day`
console.log(calculate)


const multiline =  `amar shonar bangla          
ami tomay valobasi`                         /**this method's no need to line break */
console.log(multiline)